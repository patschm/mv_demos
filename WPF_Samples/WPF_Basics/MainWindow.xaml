<Window x:Class="WPF_Basics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Basics"
        mc:Ignorable="d"
        Title="MainWindow" Height="280" Width="460">
    <Grid Margin="10"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
       

        <Label>Mooie Tekst</Label>
        <ListBox Grid.Row="1"></ListBox>
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
            <TextBox></TextBox>
            <Button Margin="0,5,0,0">Add Name</Button>
            <Button Margin="0,5,0,0" Click="Button_Click">Play Video</Button>
            <Button Margin="0,5,0,0" Click="Button_Fancy">Fancy</Button>
            <Button Margin="0,5,0,0" Click="Button_Styles">Stylish</Button>
            <Button Margin="0,5,0,0" Click="Button_MVVM">MVVM</Button>
        </StackPanel>
    </Grid>
</Window>
